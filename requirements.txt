yt-dlp>=2024.1.1
PySide6>=6.6.0

# Note for packaging: PyInstaller's PySide6 hooks lag new Qt releases. If a
# packaged build fails with "DLL load failed while importing QtCore" while
# `python main.py` runs fine, update PyInstaller before touching this file --
# the hook, not Qt, is usually what is out of date.
