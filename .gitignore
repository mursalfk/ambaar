__pycache__/
*.py[cod]
.venv/
venv/
env/
build/
dist/
*.egg-info/
*.spec.bak
.DS_Store
Thumbs.db
assets/fonts/*.ttf
assets/fonts/*.otf
# Regenerated by packaging/make_icons.py during every build, so it does not
# need to be in version control. assets/brand/ IS tracked -- the README header
# points at it, and branding.py falls back to it when icons are absent, which
# keeps a fresh clone from launching with a blank window icon.
assets/icons/generated/

# Build artefacts and local state
*.log
.idea/
.vscode/
*.rar
